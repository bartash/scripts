# https://stackoverflow.com/questions/12055198/find-out-git-branch-creator/19135644
#
# list branches in git
#
# Note that this shows date of last commit in a branch so a new branch shows the
# owner/date of the last commit in the parent branch.
git for-each-ref --format='%(committerdate)%09%(authorname)%09%(refname)' --sort=-authordate

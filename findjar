# find jars containing a string
if [ "$#" -ne 1 ]
then
 echo "usage $0 regexp"
 exit 1
fi
echo findjar
for i in `find . -name '*.jar' -print`; 
do 
  jar tvf $i | grep -i $1 &&  echo match in $i; 
done
